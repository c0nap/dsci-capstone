# Docker Build Files

This directory contains all Dockerfiles used to build service containers:
- Python containers (Boss and Workers)
- Blazor server container

The db-init/ folder contains initialization scripts executed when launching database containers.
